server:
  port:

spring:
  application:
    name:

eureka:
  instance:
    hostname: localhost
    appname: EMQX
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:8761/eureka
    healthcheck:
      enabled: false

